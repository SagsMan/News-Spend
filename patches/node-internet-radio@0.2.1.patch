diff --git a/lib/icystream.js b/lib/icystream.js
index f481aa00cfd96cf0905a7355b9b6e29bd9cc778a..d04302e7b08ead30bd5730a13f93b2d89d4929d0 100644
--- a/lib/icystream.js
+++ b/lib/icystream.js
@@ -1,71 +1,71 @@
-var urlParser = require('url');
-const tls = require('tls');
-const net = require('net');
-var utils = require('./utils.js');
+var urlParser = require("url");
+const tls = require("tls");
+const net = require("net");
+var utils = require("./utils.js");
 
-var packageJson = require('../package.json');
+var packageJson = require("../package.json");
 var versionNumber = packageJson.version;
-var clientName = 'node-internet-radio v' + versionNumber;
+var clientName = "node-internet-radio v" + versionNumber;
 
 function getStreamStation(url, callback) {
   var urlString = url;
   var completed = false;
-  var buffer = '';
+  var buffer = "";
   var maxBufferSize = 100000;
 
   // Failure timer
   var timeout = setTimeout(function () {
     tearDown();
     return callback(
-      new Error('Attempting to fetch station data via stream timed out.')
+      new Error("Attempting to fetch station data via stream timed out.")
     );
   }, 5000);
 
   var url = urlParser.parse(url);
   var headers =
-    'Icy-Metadata: 1\r\nUser-Agent: ' +
+    "Icy-Metadata: 1\r\nUser-Agent: " +
     clientName +
-    '\r\nhost: ' +
+    "\r\nhost: " +
     url.hostname +
-    '\r\n';
+    "\r\n";
 
   // Support HTTP Basic auth via Username:Password@host url syntax
   if (url.auth) {
-    var encodedAuth = new Buffer(url.auth).toString('base64');
-    headers += 'Authorization: Basic ' + encodedAuth + '\r\n';
+    var encodedAuth = new Buffer(url.auth).toString("base64");
+    headers += "Authorization: Basic " + encodedAuth + "\r\n";
   }
 
-  var getString = 'GET ' + url.path + ' HTTP/1.0\r\n' + headers + '\r\n\r\n';
+  var getString = "GET " + url.path + " HTTP/1.0\r\n" + headers + "\r\n\r\n";
 
-  if (url.protocol === 'http:') {
+  if (url.protocol === "http:") {
     var port = url.port || 80;
 
     var client = new net.Socket();
     client.setTimeout(5);
-    client.setEncoding('utf8');
+    client.setEncoding("utf8");
     client.connect(port, url.hostname, function () {
       client.write(getString);
     });
-  } else if (url.protocol === 'https:') {
+  } else if (url.protocol === "https:") {
     var port = url.port || 443;
     var client = tls.connect(
       port,
       url.hostname,
-      { ecdhCurve: false, servername: url.hostname },
+      { ecdhCurve: "auto", servername: url.hostname },
       function () {
         client.write(getString);
       }
     );
   } else {
     const error = new Error(
-      'Unknown protocol: ' + url.protocol + '. Unable to fetch stream.'
+      "Unknown protocol: " + url.protocol + ". Unable to fetch stream."
     );
     return errorCallback(error);
   }
 
-  client.on('data', dataCallback);
-  client.on('error', errorCallback);
-  client.on('close', closeCallback);
+  client.on("data", dataCallback);
+  client.on("error", errorCallback);
+  client.on("close", closeCallback);
 
   function dataCallback(response) {
     var title = null;
@@ -127,10 +127,10 @@ function getStreamStation(url, callback) {
   }
 
   function getDetailsFromBuffer(buffer) {
-    var startSubstring = 'StreamTitle=';
+    var startSubstring = "StreamTitle=";
     var startPosition = buffer.indexOf(startSubstring);
-    var endSubstring = ';';
-    var endPosition = buffer.toString().indexOf(';', startPosition);
+    var endSubstring = ";";
+    var endPosition = buffer.toString().indexOf(";", startPosition);
 
     if (startPosition > -1 && endPosition > startPosition) {
       var titleString = buffer.substring(startPosition, endPosition);
@@ -142,20 +142,21 @@ function getStreamStation(url, callback) {
   }
 
   function getHeadersFromBuffer(buffer) {
-    var headersArray = buffer.split('\n');
+    var headersArray = buffer.split("\n");
     var headersObject = {};
 
     headersArray
       .filter(function (line) {
         return (
-          (line.indexOf('icy') !== -1 && line.indexOf(':') !== -1) ||
-          line.toLowerCase().indexOf('content-type') !== -1
+          (line.indexOf("icy") !== -1 && line.indexOf(":") !== -1) ||
+          line.toLowerCase().indexOf("content-type") !== -1
         );
       })
       .forEach(function (line) {
         var keyValueArray = line.trim().split(":");
         if (keyValueArray.length === 2) {
-          headersObject[keyValueArray[0].toLowerCase()] = keyValueArray[1].trim();
+          headersObject[keyValueArray[0].toLowerCase()] =
+            keyValueArray[1].trim();
         }
       });
 
@@ -170,21 +171,21 @@ function getStreamStation(url, callback) {
 
     var station = {};
     station.title = title;
-    station.fetchsource = 'STREAM';
+    station.fetchsource = "STREAM";
     station.headers = headers;
 
     return callback(null, station);
   }
 
   function handleRedirect(buffer) {
-    var redirectTest = /Location: (.*)/mi.exec(buffer);
+    var redirectTest = /Location: (.*)/im.exec(buffer);
     if (redirectTest) {
       // Redirect!
       var newUrl = redirectTest[1];
 
       if (newUrl === urlString) {
         const error = new Error(
-          'Redirect loop detected. ' + urlString + ' -> ' + newUrl
+          "Redirect loop detected. " + urlString + " -> " + newUrl
         );
         return errorCallback(error);
       }
@@ -207,7 +208,7 @@ function getStreamStation(url, callback) {
 
   function returnError() {
     tearDown();
-    return callback(new Error('Error fetching stream'));
+    return callback(new Error("Error fetching stream"));
   }
 }
 
diff --git a/lib/shoutcast.js b/lib/shoutcast.js
index 4bf217c7d1ede789bd589c0e9d4a6399f4cce440..6bd70e3d4da8f1af9ccbd8f10247d47afccfbfe4 100644
--- a/lib/shoutcast.js
+++ b/lib/shoutcast.js
@@ -1,29 +1,29 @@
-var request = require('request');
-var parseXmlString = require('xml2js').parseString;
-var urlParser = require('url');
-var utils = require('./utils.js');
+var request = require("request");
+var parseXmlString = require("xml2js").parseString;
+var urlParser = require("url");
+var utils = require("./utils.js");
 
 function getShoutcastV1Station(url, callback) {
-  url = url + '/7.html';
+  url = url + "/7.html";
 
   var res = request(
     {
       url: url,
       headers: {
-        'User-Agent':
-          'Mozilla/5.0 (Windows; U; Windows NT 5.1; en-US; rv:1.8.1.13) Gecko/20080311 Firefox/2.0.0.13'
-      }
+        "User-Agent":
+          "Mozilla/5.0 (Windows; U; Windows NT 5.1; en-US; rv:1.8.1.13) Gecko/20080311 Firefox/2.0.0.13",
+      },
     },
-    function(error, response, body) {
+    function (error, response, body) {
       if (error) {
         return callback(error);
       }
 
       if (response.statusCode !== 200) {
-        return callback(new Error('HTTP error.'));
+        return callback(new Error("HTTP error."));
       }
 
-      res.on('error', function(error) {
+      res.on("error", function (error) {
         res.abort();
         return callback(error);
       });
@@ -31,11 +31,11 @@ function getShoutcastV1Station(url, callback) {
     }
   );
 
-  res.on('response', function(response) {
-    var contentType = response.headers['content-type'];
-    if (contentType != 'text/html') {
+  res.on("response", function (response) {
+    var contentType = response.headers["content-type"];
+    if (contentType != "text/html") {
       res.abort();
-      return callback(new Error('Not valid metadata'));
+      return callback(new Error("Not valid metadata"));
     }
   });
 }
@@ -44,26 +44,25 @@ function getShoutcastV2Station(url, callback) {
   var urlObject = urlParser.parse(url);
   var v2StatsUrl =
     urlObject.protocol +
-    '//' +
+    "//" +
     urlObject.hostname +
-    ':' +
-    urlObject.port +
-    '/statistics';
+    (urlObject.port ? ":" + urlObject.port : "") +
+    "/statistics";
 
   var res = request(
     {
-      url: v2StatsUrl
+      url: v2StatsUrl,
     },
-    function(error, response, body) {
+    function (error, response, body) {
       if (error) {
         return callback(error);
       }
 
       if (response.statusCode !== 200) {
-        return callback(new Error('HTTP error.'));
+        return callback(new Error("HTTP error."));
       }
 
-      res.on('error', function(error) {
+      res.on("error", function (error) {
         res.abort();
         return callback(error);
       });
@@ -76,17 +75,17 @@ function getShoutcastV2Station(url, callback) {
 function parseV1Response(body, callback) {
   var csvArrayParsing = /<body>(.*)<\/body>/im.exec(body);
 
-  if (!csvArrayParsing || typeof csvArrayParsing.length !== 'number') {
+  if (!csvArrayParsing || typeof csvArrayParsing.length !== "number") {
     return callback(null, null);
   }
 
-  var csvArray = csvArrayParsing[1].split(',');
+  var csvArray = csvArrayParsing[1].split(",");
   var title = undefined;
 
   if (csvArray && csvArray.length == 7) {
     title = csvArray[6];
   } else {
-    title = utils.fixTrackTitle(csvArray.slice(6).join(','));
+    title = utils.fixTrackTitle(csvArray.slice(6).join(","));
   }
 
   if (title) {
@@ -94,18 +93,18 @@ function parseV1Response(body, callback) {
     station.listeners = csvArray[0];
     station.bitrate = csvArray[5];
     station.title = title;
-    station.fetchsource = 'SHOUTCAST_V1';
+    station.fetchsource = "SHOUTCAST_V1";
 
     return callback(null, station);
   } else {
     return callback(
-      new Error('Unable to determine current station information.')
+      new Error("Unable to determine current station information.")
     );
   }
 }
 
 function parseV2Response(url, body, callback) {
-  parseXmlString(body, function(error, result) {
+  parseXmlString(body, function (error, result) {
     if (error) {
       return callback(error);
     }
@@ -130,7 +129,7 @@ function parseV2Response(url, body, callback) {
       station.listeners = stationStats.CURRENTLISTENERS[0];
       station.bitrate = stationStats.BITRATE[0];
       station.title = utils.fixTrackTitle(stationStats.SONGTITLE[0]);
-      station.fetchsource = 'SHOUTCAST_V2';
+      station.fetchsource = "SHOUTCAST_V2";
       return callback(null, station);
     } else {
       return callback(error);
